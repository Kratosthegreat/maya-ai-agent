# קריירה — מפת הפרויקט

משחק ניהול קריירת כדורגל בעברית, מוגש כדף HTML יחיד.
**המסמך הזה נטען אוטומטית בתחילת כל שיחה — קרא אותו לפני שאתה מחפש
קבצים.** אם משהו כאן לא נכון יותר, תקן אותו באותו קומיט.

📖 **`HISTORY.md` — קרא אותו לפני שאתה מתחיל משימה.** כאן המפה
(איפה כל דבר); שם ההיסטוריה: מה נבנה ומתי, כל תלונה של המשתמש ומה
נעשה בה, מאיפה בא כל מספר כיול, ומה כבר ניסינו ולא עבד. זה חוסך
גילוי מחדש — ומונע פתרון של בעיה שכבר נפתרה אחרת.

---

## הכלל שאסור לשבור: שני מנועים בתאום

לוגיקת המשחק קיימת **פעמיים**, וכל שינוי חייב להיכנס לשתיהן:

| פייתון (מקור האמת)          | JS (מה שרץ בדפדפן)   |
|-----------------------------|----------------------|
| `football_manager/*.py`     | `web/*.js`           |
| `test_football_manager.py`  | `web/test.js`        |

שמות הפונקציות זהים עד המרת snake_case ↔ camelCase.
`player.detail` בפייתון = `p.detail` ב-JS; `contract.years_left` = `contract.yearsLeft`.

**המנועים לא חולקים RNG.** אין ציפייה להתאמה מספרית מדויקת ביניהם —
רק להתנהגות זהה. אל תבזבז זמן על רדיפת הפרש של יחידה אחת.

**כן חשוב:** `int()` בפייתון קוטע, `Math.round` ב-JS מעגל. השתמש
ב-`Math.floor` כשהמקור הוא `int()`.

---

## פקודות (בסדר הנכון)

```bash
python3 web/gen_data.py      # football_manager/data.py → web/data.js   (רק אם data.py השתנה)
python3 web/build.py         # web/*.js → web/index.html                (חובה לפני פרסום)
node web/test.js             # ~2 דק'
python3 -m pytest test_football_manager.py -q   # ~8-10 דק' — הרץ ברקע
```

בדיקת דפדפן אמיתי (שמירה, קידוד, מסכים):
```bash
cd web && python3 -m http.server 8099 --bind 127.0.0.1 &
cd /home/user/maya-ai-agent
FM_URL=http://127.0.0.1:8099/index.html \
NODE_PATH=<scratchpad>/node_modules node web/test_save_browser.js
```
playwright מותקן ב-scratchpad, כרומיום ב-`/opt/pw-browsers/`.
בלי playwright הבדיקה מדלגת על עצמה.

**פרסום:** תמיד `url: https://claude.ai/code/artifact/27c71ef3-14ce-4e06-8366-9ed6c36bdb79`
(אחרת נוצר ארטיפקט חדש). לפני פרסום ראשון בשיחה — `action: "read"` על
ה-URL, אחרת הפרסום נדחה.

**ענף:** `claude/football-manager-game-al0742`.

---

## איפה כל דבר

### ליבה
| נושא | פייתון | JS |
|---|---|---|
| מצב המשחק, לולאת השבוע | `game.py` | `game.js` |
| שחקן, מועדון, ייצור עולם | `models.py` | `engine.js` |
| סימולציית משחק | `engine.py` | `engine.js` |
| טבלאות נתונים | `data.py` | `data.js` (נוצר) |
| ממשק | `cli.py` | `ui.js` |

### מערכות
| נושא | פייתון | JS |
|---|---|---|
| אימון, גדילה, הזדקנות | `progression.py` | `engine.js` |
| הסבר תכונות, תחזיות | `coaching.py` | `coaching.js` |
| **שוק העברות + משא ומתן** | `transfers.py` | `transfers.js` |
| **ציד ילדים + הורים** | `youth.py` | `youth.js` |
| **עיתונות ושמועות** | `press.py` | `press.js` |
| **שם עולמי, מיזמים** | `fame.py` | `fame.js` |
| סקאוטינג בוגרים | `scouting.py` | `scouting.js` |
| חסויות | `commercial.py` | `commercial.js` |
| נדל"ן והשקעות | `wealth.py` | `wealth.js` |
| מנטור | `mentor.py` | `mentor.js` |
| מאמן והוראות | `manager.py` | `manager.js` |
| מסלול פיתוח | `development.py` | `development.js` |
| טקטיקה | `tactics.py` | `tacticsteam.js` |
| עלילה | `story.py`, `story_pack.py`, `story_engine.py` | `story.js` |
| שמירה ודחיסה | — | `save.js` |

`web/build.py` מכיל את `JS_PARTS` — **קובץ JS חדש חייב להירשם שם
וגם ב-`web/test.js`**, אחרת הוא לא נכלל בבנייה.

---

## החלטות ארכיטקטורה שכבר נסגרו

**`detail` הוא מקור האמת.** 47 תכונות בסולם 1-20 ב-`player.detail`.
שבע הקבוצות ב-`player.attributes` (1-99) **נגזרות** מהן דרך
`recompute_groups()`. כתיבה ישירה ל-`attributes` תימחק. כל שינוי עובר
דרך `add_detail` / `add_group` / `set_group` / `set_all`.

**`overall` הוא נגזרת משוקללת לפי עמדה** (`POSITION_WEIGHTS`) — לא ממוצע.
לכן מיצוי כמה תכונות מפתח מקפיא את הדירוג. זה מה שגרם לתלונה
"נתקעתי ב-94"; הפתרון היה `ceiling_damper` + גלישת אימון + דחיפת תקרה.

**תקרות:** תכונה בודדת נעצרת ב-20 (`D.MAX_DETAIL`). `ceiling_damper`
מאט מ-16 ומעלה כך ש-20 הוא הישג של קריירה. `player.ceiling` הוא חסם
הדירוג, ועונה יוצאת דופן דוחפת אותו עד `ABSOLUTE_CEILING = 99`.

**שמירה:** localStorage הוא **הראשי** (סינכרוני, שורד סגירה פתאומית),
IndexedDB תוספת. לכל פנייה ל-IDB יש שעון עצר — פנייה תקועה לא נכשלת,
היא פשוט לא חוזרת, וזה מה שהרג שמורות בעבר. קידוד `fm3:` = 15 סיביות
לתו (base64 בזבז פי 2.6). `navigator.storage.persist()` נקרא בעלייה.

**דגלים:** מצב מערכות יושב ב-`game.flags` ונשמר אוטומטית. מפתחות
בשימוש: `offers`, `youth_offers`, `youth_interest`, `family`, `press`,
`venture`, `ventures`, `royalties`, `academy_deal`, `squad_role`,
`release_clause`, `directive`, `school`.

---

## מלכודות שכבר נפלתי בהן

1. **בדיקות רגישות לזרע.** כל קוד חדש שצורך RNG מזיז את הזרם ומפיל
   בדיקות שנשענות על 3-4 זרעים. אל תרפה את הטענה — הרחב למספר זרעים
   ומדוד את התכונה עצמה. תמיד השווה לבסיס לפני שאתה מאשים את עצמך:
   `git stash push <קבצים> && <מדידה> && git stash pop`.

2. **הגרל לפני שאתה בונה.** `weekly_press` בנה עשרה סיפורים ואז זרק
   את כולם ב-80% מהשבועות. סדר נכון: הגרלה → בנייה.

3. **שלבי קריירה.** `youth` → `academy` → `player` → `veteran`.
   מערכת שרצה רק ב-`player` לא תיראה בגיל 15. בדוק את השלב בבדיקות
   ידניות (התחל בגיל 22 לשוק בוגרים, 13 לאקדמיות).

4. **`web/index.html` נבנה ואינו נערך ידנית.** גודל ~1.5MB.

5. **קידוד.** ל-`index.html` יש `<meta charset="utf-8">` בשורה הראשונה
   כי הוא מוגש גם משרת רגיל (NAS). אל תסיר.

6. **סקריפטי עריכה בפייתון:** תמיד `assert old in s` לפני `replace`,
   ובדוק אחרי. עריכה שקטה שלא תפסה כבר עלתה כאן זמן.

---

## מוסכמות כתיבה

- הערות ותיעוד **בעברית**, וגם טקסט המשחק.
- הערה מסבירה **למה**, לא מה. "בלי זה X קורה" עדיף על "מחשב את Y".
- שמות בדיקות הם משפטים שמתארים התנהגות, לא שמות פונקציות.
- מספרי כיול מקבלים הערה שמסבירה מאיפה הם.

---

## תלונות המשתמש

הרשימה המלאה — כל תלונה, מה נעשה בה ובאיזה קומיט — נמצאת ב-`HISTORY.md`.

**המשתמש מודד לפי מה שהוא רואה במשחק.** שינוי מנוע בלי מסך שמראה
אותו נחשב אצלו כ"לא קרה כלום" — עדכן גם את הרשימה במסך הפתיחה
(`screenMenu` ב-`ui.js`) כשמוסיפים מערכת.

**ותחזק את `HISTORY.md`:** כל קומיט שמוסיף מערכת או משנה כיול מקבל
שם שורה, באותו קומיט.
