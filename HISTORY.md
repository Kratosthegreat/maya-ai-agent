# קריירה — יומן הפיתוח

מה נבנה, מתי, ולמה. `CLAUDE.md` הוא **המפה** (איפה כל דבר נמצא);
המסמך הזה הוא **ההיסטוריה** (איך הגענו לכאן ומה כבר ניסינו).

**למה זה קיים:** בלי זה כל שיחה חדשה מתחילה מאפס — קוראת קבצים,
מגלה מחדש למה מספר הוא מה שהוא, ולפעמים פותרת בעיה שכבר נפתרה
אחרת. אם אתה מתחיל משימה, קרא מכאן קודם.

**כלל תחזוקה:** כל קומיט שמוסיף מערכת או משנה כיול מקבל שורה כאן,
באותו קומיט.

---

## תמונת מצב

| | |
|---|---|
| ענף | `claude/football-manager-game-al0742` |
| ארטיפקט | `https://claude.ai/code/artifact/27c71ef3-14ce-4e06-8366-9ed6c36bdb79` |
| קומיטים למשחק | 32 |
| קוד | ~46,000 שורות, 28 מודולים × 2 מנועים + `widgets.js` |
| בדיקות | 206 פייתון · 203 JS · 18 דפדפן |
| גודל החבילה | ~1.71MB (`web/index.html`) |

---

## ציר הזמן

### הבסיס (24-25 באוגוסט)

| קומיט | מה |
|---|---|
| `650d038` | המשחק נולד — מנוע פייתון + CLI |
| `c97bc66` | גרסת ווב לנייד |
| `2f1498d`, `f0bd0a5` | גרפיקה: SVG ואז דמויות מצוירות |
| `2d80f76` | **קריירה מתחילה בגיל 13** |
| `f22c332` | הממשק נבנה מחדש בשפה של משחק ניהול |
| `9347b75` | מבנה ליגות אמיתי + עורך שמות |
| `1cc3521`, `8d36c76` | בחירת דמות ומספר חולצה במקום הקצאה |
| `609e5cc` | אצטדיון, תקציב, מתקנים, צוות |

### עומק (25 באוגוסט)

| קומיט | מה | הערה |
|---|---|---|
| `3ba18f9` | התפתחות, פציעות, גופים, תיקים, מאמן, כסף | חבילה גדולה |
| `0651685` | ספריית עלילה 36 → 106 צמתים | |
| `04e0e91` | ספריית עלילה → 185 | `story_pack.py` מונע נתונים |
| `4ad230e` | שישה תיקונים: אימון מורגש, סקאוטינג, מסלול פיתוח, חסויות, נכסים | |

### מבנה המקור (26 באוגוסט)

| קומיט | מה |
|---|---|
| `1c1e2b8` | **47 תכונות, 42 תפקידים, טקטיקה, אישיות, ידע מוסתר** — השינוי הארכיטקטוני הגדול |
| `b904ad6` | הסבר לכל תכונה, התפתחות נראית, מנטור שלא חוזר על עצמו |

### שמירה — שלושה סיבובים (25-27 באוגוסט)

| קומיט | מה | למה |
|---|---|---|
| `4be6788` | דחיסה, דיווח כשלים, גיבויים | שמורות נכשלו בשקט |
| `3e7f96b` | קובץ גיבוי אחד במקום ערימה | |
| `ebd1446` | IndexedDB + בייטים + נקודות שחזור | מכסת localStorage |
| `86cddd2` | **חזרה: localStorage ראשי, IDB תוספת** | ⚠️ הקודם היה רגרסיה |
| `9e35cd1` | `meta charset` + תצורת NAS | ג'יבריש בשרת רגיל |
| `4204d8b` | `persist()`, 3 נקודות שחזור | הדפדפן פינה מקור שלם |

### תוכן (26-27 באוגוסט)

| קומיט | מה |
|---|---|
| `4204d8b` | תקרה שאפשר להתקרב אליה (החלק השני של אותו קומיט) |
| `75ad852` | **שוק העברות עם משא ומתן, עיתונות, שם עולמי** |
| `25316a4` | **אקדמיות צדות ילדים בני 13, הורים שותפים** |
| `05554f5` | מסך פתיחה מעודכן, `CLAUDE.md` |
| `2e1f327` | `HISTORY.md` — המסמך הזה |
| `c84ff1a` | **הערכת פוטנציאל מתרעננת באמצע עונה** — תיקון הצניחה בגיל 16-17 |
| `2a477f3` | **זהות מועדון בכל מקום** — דגל, מדינה וליגה; פנייה מחו"ל מסומנת |
| `1394be3` | **שפת תצוגה חדשה** — `widgets.js`: טבעות, מדים, כרטיס משחק, גרף קו |
| `HASH2` | **מי מסביבך**: סוכנים, מאמן עם משקל, 30 פגישות, הורים ובת זוג, עילוי |

---

## תלונות המשתמש → מה נעשה

| התלונה (בלשונו) | הפתרון | קומיט |
|---|---|---|
| "נתקעתי ב-94 ומאז לא מתקדם" | `ceiling_damper` מ-16, גלישת אימון מתכונה מוצה, דחיפת תקרה עד 99 | `4204d8b` |
| "שיחקתי חצי שעה ואין שום שמירה" | localStorage ראשי + שעון עצר ל-IDB + קידוד `fm3:` | `86cddd2` |
| "השמירה נעלמה שוב" | `navigator.storage.persist()` — אחסון היה "מיטב המאמץ" | `4204d8b` |
| "הכל כן או לא, אין משא ומתן" | 6 סעיפים לניהול, מינוף גלוי, מועדון שקם מהשולחן | `75ad852` |
| "אין שמועות, אין תקשורת" | 7 מקורות באמינות שונה, לא כל שמועה נכונה | `75ad852` |
| "עניין הפרסום דל מאוד" | 6 סוגי מיזמים לפי שם עולמי | `75ad852` |
| "רוצה פנייה לפני גיל נוער" | צופי אקדמיות מגיל 12, מרוץ, הורים | `25316a4` |
| "אותו דף פתיחה" | הרשימה במסך הפתיחה הייתה מיושנת בשמונה מערכות | `05554f5` |
| "בגיל 16-17 ההתקדמות נעצרת מעט" | `reassess_youngster` — ההערכה התעדכנה פעם בשנה בזמן שנער גדל 9 נקודות | `c84ff1a` |
| "עירבבת מועדונים מקומיים עם מחו\"ל" | `D.club_tag` — הנתונים היו נכונים, הממשק לא הראה את ההבדל | `2a477f3` |
| "יש לי ui מתאים יותר למשחק" (עם תמונה) | `widgets.js` — טבעת דירוג, מדי כושר/חדות/מורל, כרטיס משחק עם סמלים, אריחי עונה, טבלה עם תגי מיקום, תיבת דואר, גרף התפתחות שבועי, סרגל צד במסך רחב | `1394be3` |
| "הכל צפוי וקבוע... בשלב מסוים זה משעמם" | `encounter_pack.py` — 30 פגישות מקריות שחוזרות, כל אחת עם בחירות ותוצאות אמיתיות | `HASH2` |
| "אין סוכנים שיכולים לטרוף עסקאות או לקלקל" | `agents.py` — 5 טיפוסים, קמפיין מול יעד, דליפה, דחיפה **וחבלה** שנחשפת | `HASH2` |
| "המאמן דמות שממליצה, כמעט בלי משקל" | `manager.py` — עמידה מ"מועדף" עד "בכלוב" בהרכב עצמו, הבטחות, פגישה בארבע עיניים, פיטורים שמאפסים הכול | `HASH2` |
| "לעילוי מציעים עולם ומלואו — לא רואה את זה" | `wonderkid_score`/`frenzy` — שכר ומענקים ×2.6, מועדוני עילית נכנסים למרוץ, הבטחות בכתב | `HASH2` |
| "משפחה נמחקה, בת זוג לא מוצעת" | `life.py` — גאווה ובקשות של ההורים, בן/בת זוג עם תמיכה מול זרקור, שלבים, פרידה שנמשכת | `HASH2` |
| "חוזים מיוחדים" | שלושה סעיפים חדשים למשא ומתן: התחייבות לדקות, מענק נאמנות, חבילה למשפחה | `HASH2` |
| "מה זה צמידות?" | `coaching.py` — הסבר + רלוונטיות אישית + תחזית | `b904ad6` |
| "המנטור חוזר על עצמו" | `mentor.py` עם `see()` והסלמה | `b904ad6` |
| "לא ברור איך מתפתח" | מסך התפתחות עם היסטוריה שנתית, גדילת גוף אמיתית | `b904ad6` |
| "חסויות שטוחות" | תיק חסויות רב־שנתי עם סעיפי בונוס | `4ad230e` |
| "רוצה נדל"ן והשקעות" | `wealth.py` עם מס מדורג | `4ad230e` |

---

## מספרי כיול, ומאיפה הם

מספר שמופיע בקוד בלי הסבר הוא מלכודת. אלה נמדדו, לא נוחשו:

| מה | ערך | מאיפה |
|---|---|---|
| `ceiling_damper` — טווח חופשי | עד 16 | ב-14 זה האט קריירות שלמות ופגע בסקאוטינג |
| `ceiling_damper` — עקומה | `(1-(l-16)*0.22)^1.8` | 17→0.64, 18→0.35, 19→0.14, 20→0.02 |
| בסיס אימון | 0.178 | היה 0.165; הועלה כדי שהבלם ישנה צורה ולא סכום |
| חלוקת סולם | `/5.0` | הבסיס כויל ל-1-100, התכונות הן 1-20 |
| `SCOUT_POOL_LIMIT` | 12 | ב-44 הביקורים התפזרו ואף אקדמיה לא בנתה תיק |
| `YOUTH_CHASED` / `YOUTH_JOINS` | 50 / 30 | 30 הוא מה שיוצר מרוץ ולא פנייה בודדת |
| משקל ערך ראשון של הורים | 4.5 | ב-3.0 בית ספר וכסף "פיצו" על מעבר לחו"ל בגיל 13 |
| `CHECKPOINT_LIMIT` | 3 | 5 היו 6.7MB — מועמד מצוין לפינוי |
| קידוד שמירה | 15 סיביות/תו | base64 בזבז פי 2.6 (0xD800+ שמור לסרוגייטים) |
| שעון עצר IDB | 4 שניות | פנייה תקועה לא נכשלת — היא לא חוזרת |
| `REASSESS_EVERY` / `UNTIL` | 10 שבועות / גיל 18 | פעם בעונה היה איטי מדי מול נער שגדל 9 נקודות בשנה |
| סף המרווח להערכה מחדש | 7 | מעליו יש עוד מקום לגדול ואין מה למהר |
| משקל שמועה על פנייה מחו"ל | 3.0 מול 2.4 | פנייה מחו"ל היא אירוע אחר, לא עוד שורה |
| `MOVE_CHANCE` (סוכן) | 0.30 | ב-0.30 בלי קמפיין הסוכן דיבר עם מועדון חדש כל שבוע |
| `CAMPAIGN_STEPS` | 9 שלבים | `CAMPAIGN_MAX_WEEKS`=9; ברשימה קצרה יותר הקמפיין חזר על עצמו |
| `CAMPAIGN_READY` | 74 | זה הסף של `SC.CHASED` — מועדון "מוכן" הוא מועדון שיציע בחלון |
| `SUPER_AGENT_FAME` | 72 | ב-55 שחקן 63 במועדון גדול קיבל סופר־סוכן |
| משקל מועדון ב-`_fame` | 0.14 | ב-0.25 המועדון קבע במקום השם שלך |
| `FRENZY_MIN` / גיל בעילוי | 0.50 / `+2` | בלי ה-`+2` בן 19 עם תקרה 95 לא יצר טירוף בכלל |
| ציון ניטרלי בלי משחקים | 0.4 | אפס העניש עילוי בתחילת עונה |
| `SABOTAGE_EXPOSURE` | 0.32 | חבלה שתמיד נחשפת היא לא פיתוי; שאף פעם לא — היא לא סיכון |
| `BREAKUP_MOOD` / `BREAKUP_CHANCE` | 22 / 0.16 | פרידה שהיא ודאית מתחת לסף הופכת את הקשר למונה זמן |
| `MEETING_COOLDOWN` | 6 שבועות | מאמן שדופקים לו על הדלת כל שבוע מפסיק לפתוח |
| בונוס עמידה בהרכב | ‎+6 עד ‎-8 | פחות מזה ו"בכלוב" לא נראה במגרש |
| `OVERALL_LOG_LIMIT` | 200 | גרף שבועי לקריירה שלמה; רשומה נכתבת רק כשהמספר זז |
| נקודות מינימום לגרף בית | 3 | שתי נקודות הן קו ישר — הן נראות כמו תקיעה |

---

## מה כבר ניסינו ולא עבד

1. **IndexedDB כאחסון יחיד** (`ebd1446`) — נראה נכון: מכסה גדולה,
   בייטים גולמיים. אבל פנייה תקועה לא מחזירה שגיאה, ולכן הגיבוי
   ל-localStorage לא רץ, דגל הכתיבה נשאר דלוק, וכפתור ההמשך לא הופיע.
   **המשתמש איבד חצי שעה.** בוטל ב-`86cddd2`.

2. **`ceiling_damper` שמתחיל ב-14** — הוריד את הדירוג בגיל 25 בכ-3
   נקודות ופגע בסקאוטינג. הועבר ל-16.

3. **בניית סיפורי עיתונות לפני ההגרלה** — נבנו עשרה סיפורים ונזרקו
   ב-80% מהשבועות, וגם הזיזו את זרם ה-RNG. הסדר הנכון: הגרלה → בנייה.

4. **לחשוד בעקומת הגיל כשההתפתחות נעצרת.** בגיל 16-17 העקומה יורדת
   ב-8% בלבד; מה שחנק היה הבלם שליד תקרת הפוטנציאל — המרווח נפל
   מ-8.2 ל-2.0 והבלם מ-0.90 ל-0.37. **מדוד את רכיבי הנוסחה לפני
   שאתה משנה אחד מהם.**

5. **עיגול `detail_growth` לחיסכון במקום** — נשקל ונדחה: המצטברים
   מזינים את הסימולציה, ועיגול היה משנה בשקט את העתיד בכל טעינה.

7. **סוכן שמפוצץ הודעות.** ראה "מערכת שעושה כל שבוע את אותו מהלך"
   למטה. `MOVE_CHANCE` נשאר 0.30, אבל המבנה השתנה מ"מהלך אקראי"
   ל"שלב בקמפיין" — וזה מה שפתר.

6. **ליישם את המוקאפ כפי שהוא.** התמונה שהמשתמש שלח היא פריסת
   דסקטופ רחבה עם סרגל צד, והוא משחק בטלפון. מה שנעשה: שפת התצוגה
   (טבעות, מדים, כרטיס משחק, גרפים) יושמה קודם כל לנייד, וסרגל הצד
   מופיע רק מעל 900 פיקסלים. **תמונה אי אפשר "לצרף" — צריך לממש.**

7. **עקיפת ה-CSP לטעינת תמונות חיצוניות** — סורב. הדרך הנתמכת היא
   `data:` מוטמע. גם סורב: תמונות של שחקנים אמיתיים, וסיפורים
   מומצאים על אנשים אמיתיים.

---

## למה ליפול בפעם הבאה

- **בדיקות רגישות לזרע.** כל צרכן RNG חדש מזיז את הזרם. בדיקה על
  3-4 זרעים תיפול בלי שיש באג. הרחב את המדגם ומדוד את התכונה — אל
  תרפה את הטענה. תמיד השווה לבסיס: `git stash push <קבצים>`.
- **`int()` בפייתון קוטע, `Math.round` מעגל.** השתמש ב-`Math.floor`.
- **המנועים לא חולקים RNG.** הפרש של יחידה בין פייתון ל-JS הוא רעש,
  לא באג.
- **קובץ JS חדש** חייב להירשם ב-`web/build.py` וב-`web/test.js`.
- **שלב הקריירה** (`youth`/`academy`/`player`/`veteran`) מחליט מה רץ.
  מערכת שנבדקה רק בגיל 22 לא תיראה בגיל 13.
- **המשתמש מודד לפי מה שהוא רואה.** מנוע בלי מסך = "לא קרה כלום".
- **משתנה CSS שנמחק לא מרעיש.** `--accent-fill` ו-`--surface` נעלמו
  מהפלטה בשלב כלשהו, וכל ה-SVG שהשתמש בהם — הגרפים, המגרש, סמל
  הנעל — צייר בשחור בלי שגיאה אחת בקונסולה. הם הוחזרו כ**כינויים**
  ל-`--accent`/`--panel`. משתנה חסר ב-SVG הוא באג שקט; חפש לפני מחיקה.
- **הרשמה כפולה.** `widgets.js` נרשם ב-`web/build.py` **וגם**
  ב-`web/test.js`. בלי הראשון הוא לא בחבילה, בלי השני הוא לא נבדק.
- **מערכת שעושה כל שבוע את אותו מהלך היא רעש.** הגרסה הראשונה של
  הסוכן "ייצרה עניין" אצל מועדון אקראי בכל שבוע. זה עבד, וזה נקרא
  כמו תקלה. התיקון: **קמפיין** — יעד אחד, תשעה שלבים, בכל פעם משפט
  אחר. אותה כמות קוד, חוויה אחרת לגמרי.
- **אירוע חדש הוא נתונים, לא קוד.** `encounter_pack.py` נכנס
  ל-`gen_data.py` ומשם ל-`data.js`, ושני המנועים מריצים את אותו
  מפרש. 30 פגישות עלו שורת ייצוא אחת. **אפקט** חדש כן צריך מימוש
  כפול (`story_engine.apply_effects` + `applyStoryEffects`).
- **נתונים נכונים אינם ממשק נכון.** שלוש הליגות תמיד היו מופרדות
  (`top`/`national` ישראליות, `euro` זרה), אבל שם של מועדון אירופי
  הוצג בדיוק כמו מקומי. כשמוסיפים מסך שמזכיר מועדון — `D.club_tag`
  חייב להופיע שם.
